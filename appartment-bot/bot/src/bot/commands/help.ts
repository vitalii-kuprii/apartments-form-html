import { CommandContext, Context } from 'grammy';

export async function helpCommand(ctx: CommandContext<Context>) {
  const helpMessage = `
📖 *Доступні команди:*

/start - Почати роботу з ботом
/search - Створити новий пошук
/searches - Переглянути мої пошуки
/favorites - Переглянути збережені квартири
/settings - Налаштування
/help - Показати цю довідку

💡 *Поради:*
• Можеш відправити посилання з DOM RIA - я розпізнаю фільтри автоматично
• Увімкни сповіщення щоб отримувати нові квартири першим

❓ Питання чи пропозиції? Пиши @your_support_username
`.trim();

  await ctx.reply(helpMessage, {
    parse_mode: 'Markdown',
  });
}
